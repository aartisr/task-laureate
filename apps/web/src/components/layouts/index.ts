/**
 * Layout Components & Utilities
 * 
 * Centralized exports for consistent page structure, layouts, and UI patterns
 * across the entire Laureate application.
 */

// Page layout
export { PageContainer } from './PageContainer';
export type { PageContainerProps } from './PageContainer';

// State components
export { LoadingState, EmptyState, ErrorState } from './StateComponents';
export type { LoadingStateProps, EmptyStateProps, ErrorStateProps } from './StateComponents';

// Card and grid layout
export { Card, Grid, Section } from './Card';
export type { CardProps, GridProps, SectionProps } from './Card';
