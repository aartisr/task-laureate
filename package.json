{
  "name": "laureate",
  "private": true,
  "version": "0.1.0",
  "description": "A generic, extensible TanStack task-management platform.",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "test": "npm run test -w apps/web",
    "lint": "npm run lint -w apps/web"
  },
  "devDependencies": {
    "supabase": "^2.110.0"
  }
}
